# Breathe — Event Discovery Web App

> just breathe.

## Setup

```bash
npm install && npm run dev
```

Copy `.env.local.example` to `.env.local` and fill in your Clerk keys from [dashboard.clerk.com](https://dashboard.clerk.com).
